# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'List' do
  inhibit_all_warnings!
  use_frameworks!

  # UI
  pod 'SnapKit', '~> 4.0.0'
  
  # Network and data
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'

end
